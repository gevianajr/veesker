.DS_Store
node_modules
/build
/.svelte-kit
**/.svelte-kit/
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust / Tauri
src-tauri/target/
src-tauri/gen/

# IDE
.idea/
*.swp

# Compiled sidecar binaries (built per-platform via scripts/build-sidecar.sh)
src-tauri/binaries/veesker-sidecar-*
!src-tauri/binaries/.gitkeep

# Oracle native bindings (copied from sidecar/node_modules at build time —
# scripts/copy-oracledb-bindings.ts). Oracle redistribution license applies.
src-tauri/oracledb-bindings/
.superpowers/

# Paid add-ons live in a separate private repo (../veesker-addons-private/)
# DO NOT re-add addons/ to this open-source repository.
addons/

# Veesker signing keys (NEVER commit)
.veesker/
*-key
*-key.pub
!ssh-keys/
